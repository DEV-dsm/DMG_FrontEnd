export interface IMenuType {
  name: string;
  id?: string;
  BlackIcons: string;
  whiteIcons: string;
}

export interface IUserInfoType {
  name: string;
  id: number;
}

export interface UserInfoInputType {
  Identity: number;
  Name: string;
  Number: number;
}
